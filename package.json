{
  "name": "angler",
  "version": "1.0.0",
  "description": "user functions for ficion",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/fishin/angler"
  },
  "author": "Lloyd Benson",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/fishin/angler/issues"
  },
  "homepage": "https://github.com/fishin/angler",
  "engines": {
    "node": ">=0.12.x"
  },
  "scripts": {
    "clean": "rm -rf node_modules",
    "console": "lab -a code -t 100 -v -m 10000 -L",
    "test": "lab -a code -t 100 -v -m 10000 -L -r json -o lab.json -r html -o lab.html -r console -o stdout -r console -o console.log"
  },
  "dependencies": {
    "bcrypt": "0.8.x",
    "hapi": "8.x.x",
    "hoek": "2.x.x",
    "pail": "git+https://github.com/fishin/pail"
  },
  "devDependencies": {
    "code": "1.x.x",
    "lab": "5.x.x"
  }
}
